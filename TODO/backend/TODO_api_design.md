# TODO — API Design (Molecular Property Prediction)

## Ендпоінти
- [ ] FastAPI-застосунок, ендпоінт `POST /predict` (SMILES → властивості) — **P0** | ~3h
- [ ] Ендпоінт `GET /health` для перевірки живості сервісу — **P0** | ~30хв
- [ ] Ендпоінт `GET /model/info` (версія активної моделі, метрики, дата навчання) — **P1** | ~1h
- [ ] (Опційно) Ендпоінт `POST /predict/batch` для декількох SMILES за раз — **P2** | ~2h

## Схеми та валідація
- [ ] Pydantic-схеми запиту/відповіді для `/predict` — **P0** | ~1h
- [ ] Валідація вхідного SMILES на рівні схеми (базовий формат) і на рівні RDKit (400 при невалідному рядку) — **P0** | ~2h
- [ ] Обмеження довжини вхідного SMILES та розміру batch-запиту — **P1** | ~1h
- [ ] Англомовні повідомлення про помилки API та опис OpenAPI/Swagger-документації (згідно з мовною політикою проєкту) — **P1** | ~1h

## Інференс
- [ ] Завантаження навченої моделі при старті застосунку (не на кожен запит) — **P0** | ~2h
- [ ] Ізоляція inference-логіки в окремому модулі, незалежному від FastAPI — **P0** | ~2h

### Примітки
- Тримати inference-логіку окремо від API-шару, щоб модель можна було тестувати без FastAPI та перевикористати в майбутніх проєктах серії.

### Залежності
- Ендпоінт `/predict` і inference-модуль напряму залежать від "production"-моделі та формату серіалізації з `ml/TODO_model_registry.md`.
- `backend/TODO_testing.md` тестує саме inference-модуль, описаний тут.
