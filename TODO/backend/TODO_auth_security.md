# TODO — Автентифікація та безпека (Molecular Property Prediction)

## Обмеження доступу
- [ ] Rate limiting на `/predict` (наприклад, slowapi) для захисту публічного demo — **P1** | ~2h
- [ ] Простий API-key або basic auth для демонстраційного доступу (опційно для публічного demo) — **P2** | ~2h
- [ ] Обмеження CORS до дозволених origin (frontend-домен) — **P0** | ~1h

## Санітизація вводу
- [ ] Санітизація вхідного SMILES проти ін'єкцій/зловживання (обмеження символів, довжини) — **P0** | ~1h
- [ ] Захист від зловживання batch-ендпоінтом (ліміт кількості молекул за запит) — **P1** | ~1h
- [ ] Валідація/екранування будь-яких параметрів, що потрапляють у SQL-запити (через ORM, без raw SQL) — **P0** | ~1h

### Примітки
- SMILES-рядок сам собою не є SQL/command injection вектором, але вхід все одно варто трактувати як недовірений (довжина, спецсимволи, DoS через важкі молекули).

### Залежності
- Rate limiting та CORS застосовуються поверх ендпоінтів з `backend/TODO_api_design.md` і мають бути готові до `devops/TODO_infrastructure_deployment.md` (публічний deploy).
