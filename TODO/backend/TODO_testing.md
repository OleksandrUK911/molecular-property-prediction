# TODO — Тестування Backend (Molecular Property Prediction)

## Unit-тести
- [ ] Unit-тести для препроцесингу (парсинг SMILES, генерація дескрипторів) — **P0** | ~2h
- [ ] Unit-тести для inference-модуля (завантаження моделі, передбачення на відомих прикладах) — **P0** | ~2h
- [ ] Unit-тести для Pydantic-схем (валідні/невалідні вхідні дані) — **P1** | ~1h

## Інтеграційні тести
- [ ] Інтеграційні тести для `/predict` (валідний SMILES → коректна структура відповіді) — **P0** | ~2h
- [ ] Інтеграційні тести для обробки помилок (`/predict` з невалідним SMILES → 400) — **P0** | ~1h
- [ ] Інтеграційні тести для `/health` — **P1** | ~30хв

## Покриття
- [ ] Визначити цільове покриття тестами (орієнтир: 70-80% для core-логіки) — **P1** | ~30хв
- [ ] Налаштувати запуск тестів локально (`pytest`) і звіт покриття (`pytest-cov`) — **P1** | ~1h

### Примітки
- Пріоритет — тести на inference/препроцесинг, а не на самі FastAPI-роути: саме там найбільша ціна помилки.

### Залежності
- Тести inference-модуля залежать від готової inference-функції з `backend/TODO_api_design.md`, яка своєю чергою обгортає модель з `ml/TODO_model_registry.md`.
- Прогін тестів інтегрується в `devops/TODO_cicd.md`.
