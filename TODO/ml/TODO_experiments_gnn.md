# TODO — Експерименти: Graph Neural Network (Molecular Property Prediction)

## Підготовка графового представлення
- [ ] Побудова графового представлення молекул (атоми як вузли, зв'язки як ребра) через RDKit + PyTorch Geometric — **P2** | ~1d
- [ ] Визначити набір атомних/зв'язкових ознак для вузлів і ребер графа (тип атома, заряд, тип зв'язку тощо) — **P2** | ~3h
- [ ] Побудова PyG `Dataset`/`DataLoader` для батчування молекулярних графів — **P2** | ~2h

## Архітектура
- [ ] Обрати базову GNN-архітектуру (GCN/GIN/GraphSAGE) для порівняння підходів — **P2** | ~2h
- [ ] Реалізувати просту модель (кілька graph-conv шарів + global pooling + регресійна голова) — **P2** | ~1d

## Навчання
- [ ] Налаштувати цикл навчання (loss, optimizer, early stopping на validation) — **P2** | ~3h
- [ ] Навчити першу версію GNN і зафіксувати метрики на validation — **P2** | ~3h

## Порівняння
- [ ] Порівняти GNN з табличними моделями (XGBoost/RF) за метриками та часом навчання/інференсу — **P2** | ~2h
- [ ] Зафіксувати висновок: чи виправдана складність GNN для цього датасету/задачі (додати в трекінг і model card) — **P2** | ~1h

### Примітки
- GNN тут — опційний "showcase"-компонент для демонстрації ширшого стеку навичок; для невеликого табличного датасету (ESOL-масштабу) табличні моделі, ймовірно, залишаться практичним переможцем — і це нормальний, чесний висновок для портфоліо.

### Залежності
- Незалежний від `ml/TODO_experiments_xgboost.md` і `ml/TODO_experiments_fingerprint_models.md` за реалізацією, але порівнюється з ними в `ml/TODO_experiments_tracking.md`.
- Має найнижчий пріоритет — можна пропустити без шкоди для MVP, якщо не вистачає часу.
