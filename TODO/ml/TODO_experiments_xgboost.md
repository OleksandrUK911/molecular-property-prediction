# TODO — Експерименти: XGBoost (Molecular Property Prediction)

## Підготовка даних
- [ ] Підготувати табличний feature-набір (дескриптори + опційно скальовані ознаки) під XGBoost — **P0** | ~1h
- [ ] Перевірити, чи потрібна додаткова обробка пропусків/нескінченностей у дескрипторах перед навчанням — **P1** | ~1h

## Базова модель
- [ ] Навчити XGBoost з дефолтними гіперпараметрами як перший кандидат понад baseline — **P0** | ~2h
- [ ] Порівняти першу версію XGBoost із лінійним baseline (RMSE/MAE/R²) — **P0** | ~1h

## Підбір гіперпараметрів
- [ ] Визначити сітку/простір гіперпараметрів (max_depth, learning_rate, n_estimators, subsample, colsample_bytree) — **P1** | ~1h
- [ ] Налаштувати тюнінг через Optuna (objective-функція на основі validation-метрики) — **P1** | ~3h
- [ ] Визначити бюджет тюнінгу (кількість trials/часу), щоб не перевчити на validation — **P1** | ~1h
- [ ] Запустити тюнінг і зафіксувати найкращі гіперпараметри — **P1** | ~2h

## Результати
- [ ] Навчити фінальну XGBoost-модель з найкращими гіперпараметрами на train+val (за потреби) — **P1** | ~1h
- [ ] Залогувати гіперпараметри, метрики та артефакт моделі в трекінг експериментів — **P0** | ~1h

### Примітки
- XGBoost на табличних дескрипторах — очікуваний найсильніший практичний кандидат для невеликого табличного датасету; тримати його як основний production-претендент.

### Залежності
- Потребує feature-набору з `ml/TODO_feature_engineering.md` та baseline-результатів з `ml/TODO_baseline.md` для порівняння.
- Результати логуються через інфраструктуру з `ml/TODO_experiments_tracking.md`.
