# TODO — Якість та валідація даних (Molecular Property Prediction)

## Аналіз розподілу
- [ ] Аналіз розподілу цільової змінної (гістограма, skew, викиди) — **P1** | ~2h
- [ ] Аналіз розподілу основних дескрипторів (MolWt, LogP тощо) — **P1** | ~2h
- [ ] Порівняння розподілів train/val/test між собою (переконатись, що вони подібні) — **P0** | ~1h

## Дублікати та аномалії
- [ ] Перевірка на дублікати молекул за канонічним SMILES — **P0** | ~1h
- [ ] Перевірка на суперечливі мітки для однакових молекул (одна структура — різні значення таргету) — **P0** | ~1h
- [ ] Виявлення викидів у таргеті (наприклад через IQR або z-score) і рішення, чи їх видаляти — **P1** | ~2h

## Data leakage
- [ ] Перевірка відсутності перетину молекул між train/val/test splits — **P0** | ~1h
- [ ] Перевірка, чи не потрапляють структурно ідентичні (але по-різному записані) SMILES у різні splits — **P0** | ~1h

### Примітки
- Дублікати молекул у різних датасетах — часта причина завищеного accuracy на тестовій вибірці, перевіряти обов'язково перед тим, як довіряти метрикам моделі.

### Залежності
- Виконується після базового спліту з `data/TODO_preprocessing_pipeline.md`; результати перевірок можуть вимагати повернення до нього для перегенерації спліту.
