# TODO — Стан та шар даних (Molecular Property Prediction)

## API-клієнт
- [ ] API-клієнт для звернень до FastAPI-бекенду (обгортка над fetch/axios) — **P0** | ~2h
- [ ] Централізована обробка помилок API-клієнта (мережеві помилки, 4xx, 5xx) — **P0** | ~2h

## Стан
- [ ] Обробка станів завантаження (loading) на рівні компонентів/сторінок — **P0** | ~1h
- [ ] Обробка помилок на фронтенді (невалідний SMILES, недоступний сервер) — **P0** | ~2h
- [ ] Кешування останніх передбачень на клієнті (уникнути повторних запитів для того ж SMILES) — **P2** | ~2h

## Історія
- [ ] Історія останніх запитів користувача (localStorage або через бекенд/БД) — **P1** | ~2h

### Примітки
- React + просте REST-звернення до FastAPI, без надмірного стейт-менеджменту (Redux тут явно надлишковий для MVP).

### Залежності
- API-клієнт напряму залежить від контракту ендпоінтів з `backend/TODO_api_design.md` (форма запиту/відповіді `/predict`).
