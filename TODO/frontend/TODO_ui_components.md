# TODO — UI-компоненти (Molecular Property Prediction)

## Ввід
- [ ] Поле вводу SMILES + кнопка "Predict" — **P0** | ~2h
- [ ] Приклади SMILES для швидкого тестування ("Try an example") — **P1** | ~1h
- [ ] Індикація стану завантаження (spinner) під час запиту до API — **P0** | ~1h

## Візуалізація молекули
- [ ] 2D-візуалізація молекули (RDKit → SVG/PNG, віддається бекендом) — **P1** | ~3h
- [ ] Fallback-стан, якщо візуалізацію не вдалось отримати — **P2** | ~1h

## Результати
- [ ] Картка результатів (MolWt, LogP, TPSA, передбачена властивість) — **P0** | ~2h
- [ ] Відображення довірчого інтервалу/невизначеності передбачення (якщо модель це підтримує) — **P2** | ~2h
- [ ] Компонент відображення помилки (невалідний SMILES, недоступний сервер) — **P0** | ~1h

### Примітки
- 2D-структуру молекули краще рендерити на бекенді (RDKit) і віддавати як SVG, ніж намагатись малювати хімічну структуру на фронтенді.

### Залежності
- 2D-візуалізація молекули потребує окремого ендпоінта/поля відповіді на бекенді (`backend/TODO_api_design.md`) для рендеру SVG через RDKit.
- Картка результатів відображає поля, визначені схемою відповіді `/predict` з `backend/TODO_api_design.md`.
